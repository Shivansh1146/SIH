"""
backend/tests/__init__.py
=========================
Test package for ThreatLens-Shield backend.

Phase 1: Empty.

Planned test modules:

    test_api.py          — tests the /ping and /scan Flask endpoints
    test_features.py     — unit tests for each feature extractor function
    test_predictor.py    — checks model loading, input validation, output range
    test_explainer.py    — verifies SHAP output shape and sign correctness
"""
