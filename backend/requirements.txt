# ThreatLens — Backend Dependencies
# ==================================
# Install with:
#   pip install -r requirements.txt

# ── Phase 1 / 2 — Core API ────────────────────────────────────────────────
flask==3.0.3
flask-cors==4.0.1

# ── Phase 2 — Testing ─────────────────────────────────────────────────────
pytest==8.2.2
pytest-flask==1.3.0

# ── Phase 4 — ML Training ─────────────────────────────────────────────────
scikit-learn>=1.6.0
pandas>=2.2.0
numpy>=1.26.0
joblib>=1.4.0
requests>=2.32.0

# ── Phase 5+ (uncomment when needed) ──────────────────────────────────────
# shap==0.45.1             # Phase 5 — SHAP explainability
# tldextract==5.1.2        # optional URL parser (if deeper domain parsing needed)
# xgboost==2.0.3           # optional alternative to RandomForest
